# ==========================================
# MONGODB (Production avec Atlas)
# ==========================================
spring.data.mongodb.uri=${MONGODB_ATLAS_URI}
spring.data.mongodb.auto-index-creation=false

# ==========================================
# Base URL pour les liens publics
# ==========================================
app.base-url=${APP_BASE_URL:https://api.blogpress.com}
app.frontend-url=${FRONTEND_URL:https://blogpress.com}

# ==========================================
# JWT CONFIGURATION (Production)
# ==========================================
# ?? IMPORTANT: En production, utilise des variables d'environnement
jwt.secret=${JWT_SECRET}
jwt.access-token-expiration=${JWT_ACCESS_TOKEN_EXPIRATION:3600000}
jwt.refresh-token-expiration=${JWT_REFRESH_TOKEN_EXPIRATION:604800000}

# ==========================================
# FILE UPLOAD (Production)
# ==========================================
#file.upload-dir=${FILE_UPLOAD_DIR:/app/uploads}
#file.max-size=5MB
#file.allowed-types=image/jpeg,image/png,image/gif,image/webp
file.storage.base-path=/app/uploads
file.storage.max-file-size=5242880
file.storage.allowed-types=image/jpeg,image/png,image/gif,image/webp
file.storage.profile-pictures-path=profile-pictures
file.storage.blog-covers-path=blog-covers
file.storage.blog-logos-path=blog-logos
file.storage.article-covers-path=article-covers

# ==========================================
# ADMIN (Production)
# ==========================================
# ?? IMPORTANT: Définis ces variables d'environnement en production
admin.email=${ADMIN_EMAIL}
admin.password=${ADMIN_PASSWORD}
admin.username=${ADMIN_USERNAME:admin}
admin.firstname=${ADMIN_FIRSTNAME:Super}
admin.lastname=${ADMIN_LASTNAME:Admin}

# ==========================================
# LOGGING (Production)
# ==========================================
logging.level.root=WARN
logging.level.com.kobe.blogpress_api=INFO
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN

# Logs en JSON pour parsing (ELK, Datadog, etc.)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ==========================================
# ACTUATOR (Production - Sécurisé)
# ==========================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# ==========================================
# SECURITY
# ==========================================
# Sécurité renforcée
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-exception=false

# ==========================================
# PERFORMANCE
# ==========================================
spring.data.mongodb.field-naming-strategy=org.springframework.data.mapping.model.SnakeCaseFieldNamingStrategy

# ==========================================
# CORS (Production - À ajuster selon ton frontend)
# ==========================================
# cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://yourdomain.com}